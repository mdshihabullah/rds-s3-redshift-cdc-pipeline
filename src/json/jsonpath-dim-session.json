{
  "jsonpaths": [
    "$['session_id']",
    "$['user_id']",
    "$['game_id']",
    "$['session_start_at']",
    "$['session_end_at']",
    "$['device_type']",
    "$['is_active']",
    "$['created_at']",
    "$['updated_at']",
    "$['__op']",
    "$['__ts_ms']",
    "$['__source_ts_ms']",
    "$['__source_lsn']"
  ]
}
